I have added the same file :  deploy3.sh in dev branch also to check if its allowing to merge
this changes r done in main branch, lets see if this appears in dev branch also &if its allowing to merge with dev branch also
